console.log("hello");

function add(a, b) {
  return a + b;
}

function log(message) {
  console.log("This is the message:", message);
}

// 1. Create new branch
// 2. Add and commit a change to that branch
// 3. Push the change to your remote branch (will be a new branch)
// 4. Make a pull request for your new change into your master (or main, whatever)
// 5. Sync up your local with your remote, then delete unneccessary branches
